info:
  name: "quotes by exchange "
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/quote/exchange/:exchange
  params:
    - name: exchange
      value: NASDAQ
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
