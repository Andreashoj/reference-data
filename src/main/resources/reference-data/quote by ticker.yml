info:
  name: quote by ticker
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/quote/ticker/:ticker
  params:
    - name: ticker
      value: AAPL
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
